{{- if .Values.arkdwallet.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "arkdwallet.fullname" . }}
  labels:
    {{- include "arkdwallet.labels" . | nindent 4 }}
  {{- with .Values.arkdwallet.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.arkdwallet.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.arkdwallet.ingress.tls }}
  tls:
    {{- range .Values.arkdwallet.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.arkdwallet.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "arkdwallet.fullname" $ }}
                port:
                  number: {{ $.Values.arkdwallet.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
